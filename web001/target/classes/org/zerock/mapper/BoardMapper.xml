<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace ="org.zerock.mapper.BoardMapper">
	<select id = "getList" resultType="org.zerock.domain.BoardVO">
		<![CDATA[
		select * from board_ex where no>0
		]]>
	</select>
	
	<insert id="insert">
		insert into board_ex (no, title, content,writer) 
		values (board_seq.nextval, #{title},#{content},#{writer})
	</insert>
	
	<insert id="insertSelectKey">
		<selectKey keyProperty="no" order="BEFORE" resultType="long">
			select board_seq.nextval from dual
		</selectKey>
		insert into board_ex (no, title, content,writer) 
		values (#{no}, #{title},#{content},#{writer})
	</insert>
	
	<select id="read" resultType="org.zerock.domain.BoardVO">
		select * from board_ex where no = #{no}
	</select>
	
	<delete id = "delete">
		delete from board_ex where no=#{no}
	</delete>
	
	<update id="update">
		update board_ex set title=#{title},
							content=#{content},
							writer=#{writer},
							updatedate=sysdate
							where no=#{no}
	</update>
</mapper>
